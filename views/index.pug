html(lang="en")
    head
        title File Metadata Microservice
        link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")
body
    div(class="container-fluid")
        h1(class="header")
        FreeCodeCamp API: File Metadata Microservice
        blockquote
            p User stories:
                ol
                    li I can submit a FormData object that includes a file upload.
                    li When I submit something, I will receive the file size in bytes within the JSON response
        
        h3 Submit a file to view its filesize:
        form(action="/getfilesize" method="post" enctype="multipart/form-data" class="form-inline" role="form")
            div(class="form-group")
                input(type="file" name="testFile")
                input(type="submit")